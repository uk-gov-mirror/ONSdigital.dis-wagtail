[tool.project]
name = "cms"
version = "0.1.0"
description = "The Wagtail CMS for managing and publishing content for the Office for National Statistics (ONS)"
authors = ["ONSdigital"]
license = "MIT"
readme = "README.md"

[tool.poetry]
package-mode = false
requires-poetry = "2.4.1"

[tool.poetry.dependencies]
python = "^3.14"
django = "~6.0.7"
wagtail = "~7.4.2"
dj-database-url = "~3.1.2"
django-defender = "~0.9.8"
django-extensions = "~4.1"
django-jinja = "^2.11.0"
django-redis = "~7.0.0"
django-storages = { version = "~1.14.6", extras = ["s3"] }
gunicorn = "~26.0.0"
psycopg = "~3.3.4"
wagtail-font-awesome-svg = "^2.0"
apscheduler = "^3.11.3"
# :TODO: Migrate to wagtail-polymath (the renamed successor) once a release includes the
# configurable MathJax URL. 1.3.1 hardcodes cdnjs.cloudflare.com with no SRI, which is why
# cdnjs is in SECURE_CSP static_sources. A release with WAGTAIL_POLYMATH's mathjax_url and
# mathjax_sri lets us serve MathJax from the ONS CDN with integrity checking and drop cdnjs.
# https://github.com/wagtail-nest/wagtail-polymath/commit/4176f020a46ce05c6b150b01563034c227d81ee4
wagtailmath = "~1.3.1"
whitenoise = "~6.12.0"
slack-sdk = "^3.43.0"
django-cache-memoize = "^0.2.1"
django-iam-dbauth = "^0.2.1"
requests = "^2.34.2"
django-treebeard = "^4.8.0" # Wagtail 7.4 allows < 6.0, but 5.x needs a TopicManager refactor
kafka-python = "^2.3.2"
wagtail-tinytableblock = "^0.4.5"
queryish = "^0.3"
python-json-logger = "^3.3.0"
json-log-formatter = "^1.2.1"
django-xff = "^1.5.0"
djangorestframework = "^3.17.1"
matplotlib = "^3.11.1"
numpy = "^2.5.0"  # used by matplotlib
django-ses = "^4.7.2"
aws-msk-iam-sasl-signer-python = "^1.0.2"
wagtailtables = "^0.2.2"
pyjwt = "^2.13.0"
cryptography = [
    { version = ">=48.0.1,<49.0.0", markers = "sys_platform == 'darwin' and platform_machine == 'x86_64'" },
    { version = "^50.0.0", markers = "sys_platform != 'darwin' or platform_machine != 'x86_64'" }
]
django-pglock = "^1.8.0"
wagtail-schema-org = "5.0.0"
django-permissions-policy = "^4.32.0"

[tool.poetry.group.dev.dependencies]
# :TODO: Remove pylint when ruff supports all pylint rules
djhtml = "3.0.11"  # keep version in sync with .pre-commit-config.yaml
mypy = "^1.20.2"
types-requests = "^2.33.0.20260518"
django-stubs = { version="^6.0.6", extras=["compatible-mypy"]}
pylint = "^4.0.6"
pylint-django = "^2.7.0"
ruff = "0.14.14"  # keep version in sync with .pre-commit-config.yaml

wagtail-factories = "^4.5.0"
coverage = "^7.15.0"
responses = "^0.26.2"

dslr = "^0.6.0"
django-debug-toolbar = "^7.0.0"
pudb = "^2025.1.5"
tblib = "^3.2.2"
Werkzeug = "^3.1.8"
playwright = "^1.61.0"
behave = "^1.3.3"

setuptools = "^83.0.0"
honcho = "^2.0.0"
detect-secrets = "^1.5.0"
moto = {extras = ["iam","s3"], version = "^5.2.2"}
django-migration-linter = "^6.0.0"
fakeredis = "^2.36.2"
time-machine = "^3.2.0"

[tool.ruff]
target-version = "py314"
line-length = 120
indent-width = 4

[tool.ruff.lint]
select = [
    # Enabling ALL is not recommended it will implicitly enable new rules after upgrade.
    # "ALL",

    # Ruff rules: https://docs.astral.sh/ruff/rules/

    "E",      # pycodestyle erros
    "W",      # pycodestyle warnings
    "F",      # Pyflakes
    "UP",     # pyupgrade
    "I",      # isort
    "B",      # flake8-bugbear
    "SIM",    # flake8-simplify
    "C4",     # flake8-comprehensions
    "S",      # flake8-bandit
    "D",      # pydocstyle - Enforce existing docstrings only
    "C90",    # mccabe
    "RUF",    # Ruff specific rules

    # PL - Pylint is only partially supported, we also use the pylint tool to catch all the rules.
    # It is enabled here to take advantage of the ruff's speed.
    "PL",
]
# Allow smart quotes
allowed-confusables = ["’", "‘", "“", "”"]

ignore = [
    # Conflicts with Google docstring style
    "D205",
    # Allow missing docstring, remove to enforce docstrings across the board
    "D100", "D101", "D102", "D103", "D104", "D105", "D106", "D107",
    # indentation contains tabs
    "W191",
    # Prefer explicit type rather than union (|) shorthand
    "UP007",
    # Allow complex functions
    "C901",
    # Allow non-top-level imports
    "PLC0415",
    # contextlib.suppress is slower
    "SIM105"
]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.per-file-ignores]
"**/tests/*" = [
    # Allow use of assert statements in tests
    "S101",
    # Allow what looks like hard-coded credentials
    "S106"
]
"**/functional_tests/*" = [
    # Allow use of assert statements in functional tests
    "S101",
]
"cms/*/migrations/*" = [
    "RUF012"
]
"cms/settings/*.py" = ["SIM105"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
# Global mypy options
no_implicit_optional = "True"
ignore_missing_imports = "True"
warn_unused_configs = "True"
warn_no_return = "False"
warn_unused_ignores = "True"
warn_return_any = "True"
warn_redundant_casts = "True"
disallow_untyped_defs = "True"
disallow_untyped_calls = "True"
disallow_incomplete_defs = "True"
strict_equality = "True"

plugins = "mypy_django_plugin.main"

[[tool.mypy.overrides]]
module = "*.migrations.*"
ignore_errors = true

[[tool.mypy.overrides]]
module = "*.tests.*"
ignore_errors = true

[[tool.mypy.overrides]]
module = "cms.settings.local"
ignore_errors = true

[tool.django-stubs]
django_settings_module = "cms.settings.test"

[tool.coverage.run]
source = ["cms"]
omit = [
    "manage.py",
    "conftest.py",
    "*venv/*",
    "*.mypy_cache*",
    "**/node_modules/*",
    "**/migrations/*",
    "**/tests/*",
    "cms/wsgi.py",
    "cms/urls.py",
    "cms/settings/*",
]
parallel = true
concurrency = ["multiprocessing", "thread"]

[tool.coverage.report]
show_missing = true
skip_covered = true

exclude_also = [
    # Have to re-enable the standard pragma
    "pragma: no cover",

    # Don't complain about missing debug-only code:
    "def __repr__",
    "if self.debug",
    "if settings.DEBUG",

    # Don't complain if tests don't hit defensive assertion code:
    "raise AssertionError",
    "raise NotImplementedError",

    # Don't complain if non-runnable code isn't run:
    "if 0:",
    "if __name__ == .__main__.:",

    # Nor complain about type checking
    "if TYPE_CHECKING:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
]

[tool.djlint]
ignore = "H006,H031"
